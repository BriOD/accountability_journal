<div id="page-wrapper">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <h1 class="page_header"> Create Your Journal</h1>
      </div>
      <div class="row">
        <div class="col-lg-12">
          <div class="panel panel-default">
            <div class="panel-heading">
            </div>
            <div class="panel-body">
              <div class="row">
                <div class="col-lg-6">
                  <%= form_for(@activity, role: "form")  do |f| %>
                  <div class="form-group">
                    <%= f.label :name %>
                    <%= f.text_field :name, class: "form-control" %>
                     <% if @activity.errors[:name].any? %>
                    <p class="help-block-error"><%= @activity.errors.full_messages_for(:name).to_sentence %></p>
                    <% else %>
                    <p class="help-block">What is it that you want to hold yourself accountable for each day?.</p>
                    <% end %>
                  </div>
                  <div class="form-group">
                    <%= f.label :description %>
                    <%= f.text_field :description, class: "form-control" %>
                     <% if @activity.errors[:description].any? %>
                    <p class="help-block-error"><%= @activity.errors.full_messages_for(:description).to_sentence %></p>
                    <% else %>
                    <p class="help-block">Describe your daily activity. Your personal goal, etc.</p>
                    <% end %>
                  </div>

                  <%= f.submit "Create Daily Activity", class: "btn btn-default"%>
                  <% end %>

                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
