
<div id="page-wrapper">
  <div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <% @journals.each do |journal| %>
        <ol>
          <li><%= link_to "#{journal.name}", journal_path(journal)%></li>
        </ol>
        <% end %>
      </div>
    </div>
  </div>
</div>
